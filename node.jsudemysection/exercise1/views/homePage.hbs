<h1>Home Page</h1>

<form action="/" method="POST">
    <div class="form-group">
        <label>First Name</label>
       <input type="text" class="form-control" name="fname" placeholder="First Name">
    </div>
        <div class="form-group">
        <label>First Name</label>
       <input type="text" class="form-control" name="lname" placeholder="Last Name">
    </div>
        <div class="form-group">
       <button type="submit" class="btn btn-info"><i class="fa fa-database">Add</i></button>
        </div>
    
     <table class="table">
         <tr>
         <th>firts Name</th>
         <th>Last Name</th>
         <th>Options</th>
         </tr>
          {{#each data}}
         <tr>
             <td class="{{_id}}">{{first_Name}}</td>
             <td class="{{_id}}">{{last_Name}}</td>
             {{!-- <td><button type="button" onclick="Edit('{{id}}')" >Edit</button></td>
             <td><button type="button" onclick="Delete('{{id}}')">Delete</button></td> --}}
         <td><a href="/{{this._id}}">Edit</a>
         <a href="/{{this._id}}" onclick=" return confirm('are you want to delete this record ?')">Delete</a>
          </td>
         </tr>
     {{/each}}

     </table>

</form>